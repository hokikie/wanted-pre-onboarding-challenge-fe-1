import TodoList from "@/components/todo/TodoList";
import Container from "@/components/common/Container";

const Home = () => {
  return (
    <Container>
      <TodoList />
    </Container>
  );
};

export default Home;
